
<h1> Categories: </h1>

<% @category.posts.each do |post| %>
  <li><%= link_to post.title, post %></li>
<% end %>

 <!-- <% @categories = Category.all %>
  <li> <% @categories.each do |category| %>
      <%= link_to category.name, category_path(category) %>
      <%= link_to category.posts, posts_path(category)%>
      <% end %> <br>
</li> -->
