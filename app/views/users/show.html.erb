<h1><%= @user.username %></h1><br>
<ul>
  <%= div_for @user.comments, class: "user-commented-posts" do |comment| %>
    <li><%= link_to comment.post.title, post_path(comment.post) %></li>
  <% end %>
</ul>
