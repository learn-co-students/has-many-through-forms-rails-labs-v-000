<h1>Hello <%= @user.username %></h1>

<p>Here's a list with all the posts you commented on:</p>

<ul>
  <% @user.posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>
