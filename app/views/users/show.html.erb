<h1><%= @user.username %></h1>
<h4><%= @user.username %> has commented on <%= pluralize(@user.post_count, "post") %>:</h4>
<ul>
  <% @user.posts.each do |post| %> 
    <li><%= link_to post.title, post_path(post) %></li>  
  <% end %>
</ul>
