<h1><%= @user.username %></h1>
<h4>Email: <%= @user.email %></h4>

<h4>Comments</h4>
<% @user.comments.each do |comment| %>
  <%= link_to comment.post_title, post_path(comment.post) %>
  <p>Said: <%= comment.content %></p>
<% end %>