<% # binding.pry # rspec spec/features/posts_spec.rb %>

<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  
  <%= f.label :content %>
  <%= f.text_field :content %>
  
  <%= f.collection_check_boxes :category_ids, Category.all, :id, :name %>
    
  <input name="post[category_name][]" id="post_categories_attributes_0_name">
  
  <%= f.submit %>
<% end %>