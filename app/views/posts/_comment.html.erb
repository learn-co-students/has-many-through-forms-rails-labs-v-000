
<%= form_for @comment do |f| %>

  <!-- <datalist id="comment_user_id">
     <% User.all.each do |user| %>
       <option value="<%= user.username %>">
     <% end %>
   </datalist> -->


  <%= f.label "Create new user" %><br />
  <%= f.fields_for :user, @comment.build_user do |user_attributes| %>
    <%= user_attributes.text_field :name %><br />
  <% end %>
  <%= f.text_field :user_id, id: 'comment_user_attributes_username' %><br />

  <%= f.label "Comment" %><br />
  <%= f.text_area :content %><br />
  <%= f.hidden_field :post_id %>
  <%= f.submit %>

<% end %>
