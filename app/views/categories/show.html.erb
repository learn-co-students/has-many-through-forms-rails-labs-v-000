<p>
  <h3>Category Name:</h3>
  <%= @category.name %>

  <h3>Posts:</h3>
    <ul>
      <% @category.posts.each do |post| %>
      <%= link_to post.title, post_path(post) %> <br>
    <% end %>
  </ul>
</p>