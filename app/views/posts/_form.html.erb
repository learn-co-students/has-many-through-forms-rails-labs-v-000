<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  
  <%= f.label :content %>
  <%= f.text_field :content %><br>

  <br>Categories:<br>
  <%= f.collection_check_boxes :category_ids, Category.all, :id, :name %><br>

  Create New Category:<br>
  <%= f.fields_for :categories, @post.categories.build do |categories_fields| %>
    <%= categories_fields.label :name %>
    <%= categories_fields.text_field :name %>
  <% end %><br>

  <%= f.submit %>
<% end %>

<!-- should include check boxes for us to select categories as well as a text field to create a new category -->
<!-- You should do this with a nested form so that our controller stays thin -->
<!-- typing in a Category name that already exists should not create a new category -->
