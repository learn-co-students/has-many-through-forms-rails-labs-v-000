<h1>This is <%= @user.username %>'s page.</h1>

<h3><%= @user.username %> has commented on the below posts:</h3>

<ul>
  <% @user.user_posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>
