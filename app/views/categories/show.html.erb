<h1>The "<%= @category.name %>" category has <%= pluralize(@category.posts.count, "post") %></h1>

<ul>
	<% @category.posts.each do |post| %>
		<li><%= link_to post.title, post_path(post) %></li>
	<% end %>

</ul>