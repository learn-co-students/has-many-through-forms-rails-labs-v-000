<h1><%= @user.username %></h1>

<h4>Posts:</h4>
<ul>
	
	<% @user.posts.each do |post| %>
		<li>
			<%=link_to post.title, post_path(post) %>
		</li>
	<% end %>

</ul>

<h4>Post Commented on:</h4>
<ul>
	
	<% @user.comments.each do |comment| %>
		<li>
			<%=link_to comment.post.title, post_path(comment.post) %>
		</li>
	<% end %>

</ul>