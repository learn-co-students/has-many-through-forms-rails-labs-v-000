
<%= @post.title %>
<%= @post.content %>
<%= @post.categories.each {|category| category.name} %>


<%= @post.comments.each {|comment| comment.content} %>

<%= @post.users.uniq.each do |user|  %>
  <%= link_to user.username, user_path(user)%>
<% end %>
