Leave a comment:
<%= form_for @comment do |f| %>
  <%= f.collection_select :user_id, User.all.uniq, :id, :username, {:include_blank => 'Create new user'} %><br />
  <%= f.fields_for :user_attributes, @comment.build_user do |user_fields| %>
    Username: <%= user_fields.text_field :username %><br />
  <% end %>
  <%= f.label :content %>
  <%= f.text_area :content %><br />
  <%= f.hidden_field :post_id, :value => @post.id %>
  <%= f.submit %>
<% end %>
