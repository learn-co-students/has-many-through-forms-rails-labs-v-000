<!-- users/show -->
<h1><%= @user.username %></h1>

<h3>Posts Commented On:</h3>
<ul>
  <% @user.comments.each do |comm| %>
    <li><%= link_to comm.post_title, post_path(comm.post) %></li>
  <% end %>
</ul>
