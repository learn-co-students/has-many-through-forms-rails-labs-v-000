<h1><%= @user.username %></h1>

Comments:
<% @user.comments.each do |comment| %>
  <p>
    <%= link_to comment.post.title, post_path(comment.post) %>: <br>
    <%= comment.content %>
  </p>
<% end %>
