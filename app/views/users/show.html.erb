
<%= @user.username %>

<% @user.comments.each do |comment|%>
    <%= comment.content %>
<% end %>

<% @user.posts.each do |post| %>
    <%= link_to post.title, post_path(post) %>
    <%= post.content %>
<% end %>