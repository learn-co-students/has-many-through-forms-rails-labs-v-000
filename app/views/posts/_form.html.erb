<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <br>
  <br>
  <p>Select Category:</p>
  <%= f.collection_check_boxes(:category_ids, Category.all, :id, :name) %>
  <br>
  <p>Or Create Category:</p>
  <%= f.fields_for :categories, @post.categories.build do |categories_fields| %>
    <%= categories_fields.text_field :name %>
  <% end %>
  <br>
  <br>
  <%= f.submit %>
<% end %>