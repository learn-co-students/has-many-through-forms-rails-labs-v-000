<h1><%= @category.name %></h1>
<ul>
  <% @category.posts.each do |post| %>
  <li><%= link_to post.title, post_path(post) %></li>
</ul>
<% end %>

<!-- categories show page should display all of the related posts -->
