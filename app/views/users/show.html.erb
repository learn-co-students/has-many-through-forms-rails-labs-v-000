<h1><%= @user.username %></h1>

<h3><%= @user.username %> commented on <%= pluralize(@user.posts.count, "post") %>:</h3>

<ul>
  <% @user.posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>
