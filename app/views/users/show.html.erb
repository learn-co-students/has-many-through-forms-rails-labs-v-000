<h1><%= @user.username %></h1>
<ul>
    <% @user.comments.each do |comment| %>
    <li>
      <%= link_to comment.post.title, comment.post %>
      <%= comment.content %>
    </li>
    <% end %>
</ul>
