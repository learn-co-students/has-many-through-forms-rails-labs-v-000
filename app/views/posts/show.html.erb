<h1><%= @post.title %></h1>
<p><%= @post.content %></p>

<%= form_for @comment do |f| %>

<%= f.label "content" %>
<%= f.text_field :content %>

<%= f.collection_select(:post, :content, User.all, :id, :username) %>
<% end %>
