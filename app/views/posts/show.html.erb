views/posts/show <br/>

<%= @post.title %>
<%= @post.content %>
<% @post.comments.each do |comment| %>
  <%= comment.content %>
<% end %>
<% @post.uniq_users_list.each do |user| %>
  <%= link_to user.username, user %>
<% end %>
