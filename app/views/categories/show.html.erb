<!-- # app/views/categories/show.html.erb -->

<h1><%= @category.name %></h1>

<p>Posts:</p>
<ul>
  <% @category.posts.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post) %>
      <%= post.content %>
    </li>
  <% end %>
</ul>
