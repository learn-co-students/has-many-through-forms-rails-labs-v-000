Post Title:<%= @post.title %> <br>
Post Content:<%= @post.content %> <br><br>
<%= form_for @post.comments.build do |f| %>
  Comment:<br><%= f.text_area :content %>
  User:<%=f.collection_select :user_id, User.all, :id, :username%>
  <%= f.submit "Create Comment"%>
<% end %>
