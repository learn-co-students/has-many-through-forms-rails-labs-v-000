<h1>New Post </h1>

<%= form_for @post do |f| %> 
	<%= f.label :"Title" %>
	<%= f.text_field :title %> 
	<br/>
	<h3>Categories</h3>
	<p>Choose a pre-existing category:</p>
	<%= f.collection_check_boxes :category_ids, Category.all, :id, :name%>
	<br/>
	<p>Create a New Category</p>
	<%= fields_for @post.categories.build do | cat_attr | %>
		<%= cat_attr.text_field :name, id: "post_categories_attributes_0_name", name: "post[categories_attributes][name]" %> 
	<% end %>
	<br/>
	<h3>Body</h3>
	<%= f.text_area :content, size:"50x20"%> 
	<br/><br/>
	<%= f.submit "Create Post" %> 
<% end %> 


<!-- 
	New Form Needs: 
		- checkboxes for categories 
		x submit button 'Create Post'
		- textbox to create a new category 
		- accepts new and old categories at the same time
-->
