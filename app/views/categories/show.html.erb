<% if @related_posts %>
  <%  @related_posts.all.each do |post| %>
    <p><%= link_to post.title, post_path(post) %></p>
  <% end %>
<% end %>

<%= Category.all.each do |cat| %>
  <%= cat.name %>
  <% end %>
